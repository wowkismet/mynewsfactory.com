# Public site origin, used for canonical URLs and metadata.
NEXT_PUBLIC_SITE_URL=https://mynewsfactory.com

# PostgreSQL connection (Phase 2 — not required for the front end).
# DATABASE_URL=postgresql://user:password@localhost:5432/mynewsfactory
